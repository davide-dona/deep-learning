\section{Recurrent Neural Networks}
Unlike the architectures previously discussed, which only process data represented as fixed-size vectors for both input and output,
\textbf{Recurrent Neural Networks} (RNNs) are designed to handle sequential data.

Standard feed-forward models face significant limitations with real-world data types such as text, audio, and video, which are inherently sequential and vary in length.

\textbf{Recurrent Neural Networks} (RNNs) were the first architectures specifically designed to handle
this kind of data, enabling solutions to a wide range of tasks, such as:
\begin{itemize}
    \item \textbf{Document classification}: Given a document, classify it into one of several categories (e.g., spam vs. non-spam).
    We know that the document size cannot be fixed a priori, as it can vary significantly from one document to another.
    \item \textbf{Sentiment analysis}: similar to document classification, but the goal is to classify the sentiment of a text.
    \item \textbf{Image captioning}: Given an image, generate a caption that describes the content of the image.
    This is a more complex task, as it requires the model to generate a sequence of words that describe the image, which can have variable length.
    \item \textbf{Dynamical Systems}: dynamical systems are systems that evolve over time, based on the current state, some external input,
    and a predetermined set of rules that define how the system transitions from one state to another.
    RNNs can be used to learn a non-fixed transition function that captures the underlying dynamics of the system, allowing for accurate predictions of future states based on past observations.
\end{itemize}

\subsection{RNN Architecture types}
While classical Feed-Forward Neural Networks (FFNNs) are categorized as \textbf{one-to-one} (or single-to-single)
architectures, taking one input and producing one output, RNNs are classified based on how they map input sequences to output sequences.
The primary types include:
\begin{itemize}
    \item \textbf{Many-to-One}: This type of RNN takes a sequence of inputs and produces a single output.
    This is the case of document classification, where the input is a sequence of words and the output is a single class label.
    \begin{figure}[H]
        \centering
        \input{chapters/07-rnn/tikz/Many-to-One.tex}
        \caption{Many-to-One RNN architecture.}
    \end{figure}

    \item \textbf{One-to-Many}: This type of RNN takes a single input and produces a sequence of outputs.
    This is the case of image captioning, where the input is an image and the output is a sequence of words that describe the image.
    \begin{figure}[H]
        \centering
        \input{chapters/07-rnn/tikz/One-to-Many.tex}
        \caption{One-to-Many RNN architecture.}
    \end{figure}
    \item \textbf{Many-to-Many}: This type of RNN takes a sequence of inputs and produces a sequence of outputs.
    We can have 2 subtypes of this architecture:
    \begin{itemize}
        \item \textbf{Synchronized}: The input and output sequences have the same length, and an output is generated for every input
        (e.g., Frame-level video classification or Part-of-Speech tagging).
        \item \textbf{Encoder-Decoder}: The model first processes the entire input sequence (encoding) before generating an output sequence of a potentially different length (decoding),
        as seen in Machine Translation.
    \end{itemize}
    \begin{figure}[H]
        \centering
        \begin{minipage}{0.45\textwidth}
            \centering
            \input{chapters/07-rnn/tikz/Many-to-Many-Synchronized.tex}
            \caption{Many-to-Many Synchronized RNN architecture.}
        \end{minipage}
        \hfill
        \begin{minipage}{0.45\textwidth}
            \centering
            \input{chapters/07-rnn/tikz/Many-to-Many-E-D.tex}
            \caption{Many-to-Many Encoder-Decoder RNN architecture.}
        \end{minipage}
    \end{figure}
\end{itemize}

\subsection{Vanilla RNNs}
The simplest form of RNN is the \textbf{Vanilla RNN}, which consists of a single hidden layer that is recurrently connected to itself.
\begin{figure}[H]
    \centering
    \input{chapters/07-rnn/tikz/Vanilla-RNN.tex}
    \caption{Vanilla RNN architecture.}
\end{figure}

At each time step $t$, the model combines the current input $\vct{x}_t$ with the previous hidden representation $\vct{h}_{t-1}$
to compute a new hidden representation $\vct{h}_t$.
The updated state $\vct{h}_t$ is then used to produce the output $y_t$ and is passed forward to the next step $t+1$.
By managing this recurrent connection, the model creates a memory across time steps, enabling it to capture the temporal dependencies in the input sequence.

\paragraph{Hidden representation}
The hidden representation $\vct{h}_t$ can be computed as:
\begin{equation}
    \vct{h}_t = \sigma_{\mat{W}}(\vct{x}_t, \vct{h}_{t-1})
\end{equation}
Where $\sigma_{\mat{W}}$ is a non-linear activation function (e.g., ReLU, Tanh) applied to a linear transformation of the current input $\vct{x}_t$ and the previous hidden state $\vct{h}_{t-1}$,
with $\mat{W}$ representing the learnable parameters of the model.

Taking the Tanh activation function as an example, we can express the hidden representation as:
\begin{equation}
    \vct{h}_t = \tanh(\mat{W}_{x} \vct{x}_t + \mat{W}_{h} \vct{h}_{t-1})
\end{equation}
where $\mat{W}_{x}$ and $\mat{W}_{h}$ are the weight matrices associated with the input and hidden state, respectively.

\paragraph{Unrolled Representation}
The RNN can also be unrolled in time, which means that we can represent the RNN as a feed-forward network where each layer corresponds to a time step in the sequence.
\begin{figure}[H]
    \centering
    \scalebox{0.8}{
        \input{chapters/07-rnn/tikz/Unrolled.tex}
    }
    \caption{Unrolled RNN architecture.}
\end{figure}

\subsubsection{Backpropagation Through Time}
To train RNNs, we use a variant of backpropagation called \textbf{Backpropagation Through Time} (BPTT),
which follows the same principles as standard backpropagation.

The unfolded RNN is treated as a deep feed-forward network, where each layer corresponds to a time step in the sequence.
The weights are shared across all time steps, and the error is backpropagated through the entire sequence, allowing the model to learn from the temporal dependencies in the data.

\paragraph{BPTT Example}
Let's see a simple example of how BPTT works in a Vanilla RNN:
\begin{itemize}
    \item \textbf{Forward Pass}: The input sequence $(\vct{x}_1, \vct{x}_2, \vct{x}_3)$ is fed into the RNN, producing hidden states $(\vct{h}_1, \vct{h}_2, \vct{h}_3)$ and outputs $(y_1, y_2, y_3)$.
    \item \textbf{Loss Calculation}: The loss is computed based on the outputs and the true labels for each time step.
    The error is expressed as the sum of the losses at each time step:
    \begin{equation}
        \loss = \sum_{t=1}^{T} \loss_t(y_t, \hat{y}_t)
    \end{equation}
    In our example, with $T=3$, we have:
    \begin{equation}
        \loss = \loss_1(y_1, \hat{y}_1) + \loss_2(y_2, \hat{y}_2) + \loss_3(y_3, \hat{y}_3) = \loss_1 + \loss_2 + \loss_3
    \end{equation}
    \item \textbf{Backward Pass}: in this example we will compute the gradient of the loss with respect to the weights associated with the hidden layer $\mat{W}_h$.
    Note that the process is analogous for the weights associated with the input layer $\mat{W}_x$ and the output layer $\mat{W}_y$.

    The gradient of the loss with respect to these weights is computed by summing the contributions from all time steps:
    \begin{equation}
        \frac{\partial \loss}{\partial \mat{W}_h} = \sum_{t=1}^{T} \frac{\partial \loss_t}{\partial \mat{W}_h}
    \end{equation}

    Where each term $\frac{\partial \loss_t}{\partial \mat{W}_h}$ is computed as:
    \begin{equation}
        \frac{\partial \loss_t}{\partial \mat{W}_h} = \frac{\partial \loss_t}{\partial y_t} \cdot \frac{\partial y_t}{\partial \vct{h}_t} \cdot \frac{\partial \vct{h}_t}{\partial \mat{W}_h}
    \end{equation}

    For example, computing the gradient of the error with respect to $\vct{h}_1$ (how the hidden state at time step 1 contributes to the loss) we have to consider:
    \begin{itemize}
        \item How $\vct{h}_1$ contributes to $\vct{h}_2$;
        \item How $\vct{h}_2$ effects $\vct{h}_3$;
        \item How $\vct{h}_3$ contributes to the error at time step 3.
    \end{itemize}
    This gives us:
    \begin{equation}
        \frac{\partial \loss}{\partial \vct{h}_1} = \frac{\partial \loss}{\partial \vct{h}_3} \cdot \frac{\partial \vct{h}_3}{\partial \vct{h}_2} \cdot \frac{\partial \vct{h}_2}{\partial \vct{h}_1}
    \end{equation}

\end{itemize}

\subsubsection{Challenges with BPTT: The Vanishing Gradient}

When generalizing to a sequence of length $T$, the gradient of the loss with respect to the initial hidden state $\vct{h}_1$ is computed using the chain rule:
\begin{equation}
    \frac{\partial \loss}{\partial \vct{h}_1} = \frac{\partial \loss}{\partial \vct{h}_T} \cdot \prod_{t=2}^{T} \frac{\partial \vct{h}_t}{\partial \vct{h}_{t-1}}
\end{equation}

While Backpropagation Through Time (BPTT) allows RNNs to learn from sequential data, this multiplicative chain introduces a critical flaw known as the \textbf{vanishing gradient} problem. The issue can be broken down as follows:

\begin{itemize}
    \item \textbf{The Mechanism:} Early time steps in a sequence receive gradients through a long chain of multiplications. If the sequence is long, these repeated multiplications cause the gradients to shrink exponentially as they propagate backward.
    \item \textbf{The Consequence:} The gradients for early states (like $\vct{h}_1$) become vanishingly small, which effectively prevents the model from updating its weights and learning long-term dependencies in the data.
    \item \textbf{The Real-World Impact:} This is particularly problematic for tasks that require a deep understanding of long-range context—such as language modeling or machine translation—where the model must capture relationships between words that are far apart in the input sequence.
\end{itemize}

\subsubsection{A partial solution: Truncated BPTT}
A common practical solution to the vanishing gradient problem and the high computational cost of standard BPTT is \textbf{Truncated Backpropagation Through Time} (TBPTT).
The core idea is to approximate the computation by processing the sequence in smaller chunks rather than the entire history at once.

The algorithm can be characterized by two parameters:
\begin{itemize}
    \item $k_1$: The number of time steps between gradient updates (the forward pass length).
    \item $k_2$: The maximum number of time steps backpropagated (the truncation window).
\end{itemize}
This approach allows the model to learn from recent history while mitigating the vanishing gradient problem, as we only backpropagate through a limited number of time steps,
thus. It also reduces the computational cost, allowing for more efficient training of RNNs on long sequences.

This approach is particularly effective for tasks where long-term dependencies are less critical, as in fact
it fails to capture dependencies that span more than $k_2$ time steps, but it can still be useful for many applications where recent context is more relevant.

\subsection{Long Short-Term Memory (LSTM)}
To address the limitations of Vanilla RNNs, specifically the \textbf{vanishing gradient problem}, \textbf{Long Short-Term Memory} (LSTM)
networks were introduced. LSTMs utilize a gated architecture designed to maintain a persistent state over long sequences,
allowing the model to learn when to forget old information and when to update with new inputs.

\subsubsection{The Cell State}
The core innovation of the LSTM is the \textbf{cell state} (denoted as $\vct{C}_t$), which acts as a long-term memory conveyor belt.
Unlike the hidden state $\vct{h}_t$, which undergoes a non-linear transformation at every time step, the cell state $\vct{C}_t$
interacts with the rest of the network primarly through linear operations.

This linear path allows gradients to flow through many time steps with minimal decay,
effectively creating a "highway" for gradients during backpropagation, which is crucial for learning long-term dependencies in sequential data.

\subsubsection{Gating Mechanisms}
LSTMs regulate the cell state using three specialized "gates."
Each gate consists of a sigmoid layer ($\sigma$) and an element-wise multiplication operation ($\odot$).
These gates are learned parameters that determine the flow of information.

\begin{figure}[H]
    \centering
    \resizebox{.8\textwidth}{!}{
        \input{chapters/07-rnn/tikz/LSTM.tex}
    }
    \caption{Internal gated architecture of an LSTM cell.}
\end{figure}

\paragraph{Input Gate ($\vct{i}_t$):}
The input gate decides which values in the cell state will be updated.
Simultaneously, a \textit{candidate} cell state $\tilde{\vct{C}}_t$ is created using a $\tanh$ layer to represent new information that could be added to the state.
\begin{equation}
    \vct{i}_t = \sigma\left(\mat{W}_i \cdot [\vct{h}_{t-1}, \vct{x}_t] + \vct{b}_i\right)
\end{equation}
The input gate takes the previous hidden state $\vct{h}_{t-1}$ and the current input $\vct{x}_t$ as input, applies a linear transformation followed by a sigmoid activation function,
and outputs a value between 0 and 1 for each element in the cell state, where 0 means "do not update" and 1 means "update".

On the other hand, the candidate cell state $\tilde{\vct{C}}_t$, similarly to the hidden state in a Vanilla RNN,
is computed as a non-linear transformation of the current input and the previous hidden state:
\begin{equation}
    \tilde{\vct{C}}_t = \tanh\left(\mat{W}_C \cdot [\vct{h}_{t-1}, \vct{x}_t] + \vct{b}_C\right)
\end{equation}

\paragraph{Forget Gate ($\vct{f}_t$):}
This gate decides what information from the previous cell state $\vct{C}_{t-1}$ is no longer useful and should be discarded.
\begin{equation}
    \vct{f}_t = \sigma\left(\mat{W}_f \cdot [\vct{h}_{t-1}, \vct{x}_t] + \vct{b}_f\right)
\end{equation}
It takes the previous hidden state $\vct{h}_{t-1}$ and the current input $\vct{x}_t$ as input, applies a linear transformation followed by a sigmoid activation function,
and outputs a value between 0 and 1 for each element in the cell state.
The output can be interpreted as a soft mask, where $0$ means "completely forget," while $1$ means "completely retain."

\paragraph{Output Gate ($\vct{o}_t$):}
This gate decides what information to output from the cell state.
\begin{equation}
    \vct{o}_t = \sigma\left(\mat{W}_o \cdot [\vct{h}_{t-1}, \vct{x}_t] + \vct{b}_o\right)
\end{equation}
It takes the previous hidden state $\vct{h}_{t-1}$ and the current input $\vct{x}_t$ as input, applies a linear transformation followed by a sigmoid activation function,
and outputs a value between 0 and 1 for each element in the hidden state.
The output can be interpreted as a soft mask, where $0$ means "completely forget," while $1$ means "completely retain."

\subsubsection{Memory Cell Update}
The new cell state $\vct{C}_t$ is updated by combining the gated previous state and the gated candidate state.
This is the "update" step where the network actually forgets and learns:
\begin{equation}
    \vct{C}_t = \vct{f}_t \odot \vct{C}_{t-1} + \vct{i}_t \odot \tilde{\vct{C}}_t
\end{equation}
By using the forget gate $\vct{f}_t$, the network can reset the memory of a specific unit if it is no longer relevant, preventing the internal state from growing indefinitely.

Using the input gate $\vct{i}_t$ and the candidate cell state $\tilde{\vct{C}}_t$, the network can add new information to the cell state, allowing it to learn from new inputs while retaining important past information.

\subsubsection{Hidden State Generation}
Finally, the hidden state $\vct{h}_t$ is updated.
We pass the cell state through a $\tanh$ (to push the values between $-1$ and $1$) and multiply it by the output gate $\vct{o}_t$:
\begin{equation}
    \vct{h}_t = \vct{o}_t \odot \tanh(\vct{C}_t)
\end{equation}
While $\vct{C}_t$ stores long-term information, $\vct{h}_t$ serves as the "working memory" used for the immediate prediction and as an input for the next time step.

\subsubsection{Gradients and Backpropagation}
The architectural beauty of the LSTM lies in its additive update rule for $\vct{C}_t$.
Even though the gates introduce non-linearities, the cell state $\vct{C}_t$ itself is updated through a combination of linear operations (element-wise multiplication and addition),
which provides a nearly linear path for gradients to flow during backpropagation.
This significantly mitigates the vanishing gradient problem inherent in the multiplicative chain of standard RNNs.

\subsection{Gated Recurrent Unit (GRU)}
Gated Recurrent Units (GRUs) are a simplified version of LSTMs that tries to reduce the number of parameters and computational complexity while maintaining similar performance.
GRUs achieve this by combining the forget and input gates into a single "update gate" and merge the cell state and hidden state into a single state.
This results in a more streamlined architecture that is computationally more efficient while still capturing long-term dependencies effectively.

\subsubsection{GRU Cell Architecture}
In a GRU cell, we have only two gates: the \textbf{update gate} ($\vct{z}_t$) and the \textbf{reset gate} ($\vct{r}_t$).

\begin{figure}[H]
    \centering
    \resizebox{.8\textwidth}{!}{
        \input{chapters/07-rnn/tikz/GRU.tex}
    }
    \caption{Internal gated architecture of a GRU cell.}
\end{figure}

\paragraph{Reset Gate ($\vct{r}_t$):}
The reset gate determines how much of the previous hidden state $\vct{h}_{t-1}$ should be used when computing the candidate hidden state $\tilde{\vct{h}}_t$.
\begin{equation}
    \vct{r}_t = \sigma\left(\mat{W}_r \cdot [\vct{h}_{t-1}, \vct{x}_t] + \vct{b}_r\right)
\end{equation}
It takes the previous hidden state $\vct{h}_{t-1}$ and the current input $\vct{x}_t$ as input, applies a linear transformation followed by a sigmoid activation function,
and outputs a value between 0 and 1 for each element in the hidden state, where 0 means "completely reset" and 1 means "completely retain."

\paragraph{Candidate Hidden State ($\tilde{\vct{h}}_t$):}
The candidate hidden state is computed based on the reset gate and the previous hidden state.
\begin{equation}
    \tilde{\vct{h}}_t = \tanh\left(\mat{W}_h \cdot [\vct{r}_t \odot \vct{h}_{t-1}, \vct{x}_t] + \vct{b}_h\right)
\end{equation}
The reset gate $\vct{r}_t$ controls how much of the previous hidden state $\vct{h}_{t-1}$ is used in the computation of the candidate hidden state $\tilde{\vct{h}}_t$.
If $\vct{r}_t$ is close to 0, the previous hidden state is effectively ignored, allowing the model to reset its memory when necessary.

\paragraph{Update Gate ($\vct{z}_t$):}
The update gate determines how much of the previous hidden state $\vct{h}_{t-1}$ and the candidate hidden state $\tilde{\vct{h}}_t$ should be
combined to form the new hidden state $\vct{h}_t$.
\begin{equation}
    \vct{z}_t = \sigma\left(\mat{W}_z \cdot [\vct{h}_{t-1}, \vct{x}_t] + \vct{b}_z\right)
\end{equation}
It takes the previous hidden state $\vct{h}_{t-1}$ and the current input $\vct{x}_t$ as input, applies a linear transformation followed by a sigmoid activation function,
and outputs a value between 0 and 1 for each element in the hidden state, where 0 means "completely update" and 1 means "completely retain."

The new hidden state $\vct{h}_t$ is then computed as a linear interpolation between the previous hidden state $\vct{h}_{t-1}$ and the candidate hidden state $\tilde{\vct{h}}_t$,
weighted by the update gate $\vct{z}_t$:
\begin{equation}
    \vct{h}_t = (1 - \vct{z}_t) \odot \vct{h}_{t-1} + \vct{z}_t \odot \tilde{\vct{h}}_t
\end{equation}
This allows the GRU to adaptively capture long-term dependencies while maintaining a simpler architecture compared to LSTMs.

\paragraph{LSTM vs GRU}
The LSTM and GRU architectures both address the vanishing gradient problem and are capable of capturing long-term dependencies in sequential data.
However, they differ in complexity and performance characteristics:
\begin{itemize}
    \item \textbf{Complexity:} LSTMs have three gates and a separate cell state, while GRUs have two gates and combine the cell and hidden states.
    This allows GRUs to have a lower number of parameters.
    \item \textbf{Training Speed:} GRUs are generally faster to train due to their smaller number of parameters and simpler architecture.
    \item \textbf{Performance:} LSTM were shown to be very strong in capturing long-term dependencies, while GRUs are often competitive,
    but may not perform as well on tasks that require modeling very long sequences.
\end{itemize}

\subsection{Multi-Layer RNNs}
To increase the representational capacity of RNNs, we can stack multiple RNN layers on top of each other, creating a \textbf{multi-layer RNN} (also known as a deep RNN).
In this architecture, the hidden state of each layer serves as the input to the next layer, allowing the model to learn hierarchical representations of the sequential data.

\begin{comment}
    #TODO: Add a figure showing a multi-layer RNN architecture.
\end{comment}

\subsubsection{Bi-directional RNNs}
Very popular in the speech recognition community are the Bi-directional RNNS. They allow processing input sequences in both
forward and backward directions, enabling the model to capture context from both past and future time steps.

Altough bi-directional RNNs are powerful, they are not suitable for real-time applications, as they require access to the entire input sequence before making predictions.
