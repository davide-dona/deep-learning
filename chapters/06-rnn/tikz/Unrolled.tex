\begin{tikzpicture}[
    >=stealth,
    node distance=1.2cm,
    box/.style={
        rectangle, draw, fill=rnnblock, 
        rounded corners=3pt, minimum width=2.2cm, 
        minimum height=0.7cm, thick, font=\small\bfseries
    },
    arrow/.style={->, thick},
    label_node/.style={align=center, font=\small}
]

    % --- Time Step 1 ---
    \node[box] (HL1) {Hidden layer};
    \node[box, above=of HL1] (C1) {Classifier};
    \node[above=0.4cm of C1] (Y1) {$y_1$};
    \node[label_node, below=0.6cm of HL1] (LX1) {$x_1$ \\ \textbf{\color{dlsky!70!black} t = 1}};

    % --- Time Step 2 ---
    \node[box, right=1.8cm of HL1, yshift=0.8cm] (HL2) {Hidden layer};
    \node[box, above=of HL2] (C2) {Classifier};
    \node[above=0.4cm of C2] (Y2) {$y_2$};
    \node[label_node, below=0.6cm of HL2] (LX2) {$x_2$ \\ \textbf{\color{dlsky!70!black} t = 2}};

    % --- Time Step 3 ---
    \node[box, right=1.8cm of HL2, yshift=0.8cm] (HL3) {Hidden layer};
    \node[box, above=of HL3] (C3) {Classifier};
    \node[above=0.4cm of C3] (Y3) {$y_3$};
    \node[label_node, below=0.6cm of HL3] (LX3) {$x_3$ \\ \textbf{\color{dlsky!70!black} t = 3}};

    % --- External States ---
    \node[left=1.2cm of HL1, yshift=-0.8cm] (H0) {$h_0$};
    \node[right=1.2cm of HL3, yshift=0.8cm] (H3_end) {$h_3$};

    % --- Connections ---
    \foreach \i in {1,2,3} {
        \draw[arrow] (LX\i.north) -- (HL\i.south);
        \draw[arrow] (HL\i.north) -- (C\i.south);
        \draw[arrow] (C\i.north) -- (Y\i);
    }

    % Recurrent arrows with labels
    \draw[arrow] (H0) -- node[above left] {$h_0$} (HL1.west);
    \draw[arrow] (HL1.east) -- node[above left] {$h_1$} (HL2.west);
    \draw[arrow] (HL2.east) -- node[above left] {$h_2$} (HL3.west);
    \draw[arrow] (HL3.east) -- node[above left] {$h_3$} (H3_end);

\end{tikzpicture}