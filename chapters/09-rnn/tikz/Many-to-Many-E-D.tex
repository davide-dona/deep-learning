\begin{tikzpicture}[
    item/.style={rectangle, draw, fill=rnnblock, minimum size=6mm},
    >=Stealth
]
    % Encoder Nodes (1-3)
    \foreach \i in {1,2,3} {
        \node[item] (h\i) at (\i*1.2, 0) {};
        \draw[<-] (h\i.south) -- ++(0,-0.5);
    }

    % Decoder Nodes (4-6)
    \foreach \i in {4,5,6} {
        \node[item] (h\i) at (\i*1.2, 0) {};
        \draw[->] (h\i.north) -- ++(0,0.5);
    }

    % Full Horizontal Sequence
    \draw[->] (h1) -- (h2);
    \draw[->] (h2) -- (h3);
    \draw[->] (h3) -- (h4);
    \draw[->] (h4) -- (h5);
    \draw[->] (h5) -- (h6);
\end{tikzpicture}